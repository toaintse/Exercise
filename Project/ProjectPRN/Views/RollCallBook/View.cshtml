@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@using ProjectPRN.Models

<div>
    <h1>Student's Infomation</h1>
    <table>
        <tr>
            <th>StudentName:</th>
            <td>@ViewBag.Stus.FirstName @ViewBag.Stus.MidName @ViewBag.Stus.LastName</td>
        </tr>
        <tr>
            <th>RollNo:</th>
            <td>@ViewBag.Stus.Roll</td>
        </tr>
    </table>
    <table border=1>
        <tr>
            <th>Room</th>
            <th>Slot</th>
            <th>Date</th>
            <th>Status</th>
            <th>Comment</th>
        </tr>
        @foreach (RollCallBook item in ViewBag.Students)
        {
            <tr>
                <td>@item.TeachingSchedule.Room.RoomCode</td>
                <td>@item.TeachingSchedule.Slot</td>
                <td>@(((DateTime)item.TeachingSchedule.TeachingDate).ToShortDateString())</td>
                <td>@((item.IsAbsent) == true ? "Absent" : "Attended")</td>
                <td>@item.Comment</td>
                <td><a href=""><input type="button" value="Edit"></input></a></td>
                <td><a href=""><input type="button" value="Delete"></input></a></td>
            </tr>
        }
        
    </table>
</div>